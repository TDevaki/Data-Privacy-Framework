Virtual Environment:

	Name of the virtual environment
		- projects -m venv projects
	Activate the environment
		- projects\Scripts\activate.bat
******************************************************
pip install fastapi

pip install "uvicorn[standard]"

To use forms # pip install python-multipart

Any template engine can use with Fast API common choice is Jinja2, the same one used by Flask and other tools.

pip install jinja2
